# Profile: motifpath-web

## Identification

- Repository / path scope: `motifpath/motifpath-web` (single repo).
- Local reference repo: `motifpath-web` checkout.
- Comment language: English.
- Stack in one line: Vue 3, Composition API + `<script setup lang="ts">`, TypeScript strict, Vite,
  Vitest, Tailwind, Pinia.

## Sources of intent

- Spec: OpenAPI paths in `motifpath-specs/openapi/` (consumed as generated types); business rules in
  `motifpath-specs/features/*.feature`.
- Backlog item: task code in the branch name (`feat/MTP-NNN/...`), per the `git` skill.
- Fallback: ask the user for the activity's context.

## Local norms (with evidence)

| Norm | Evidence |
|---|---|
| `src/api/generated/` is generated by `openapi-typescript` from `motifpath-specs` — never hand-edited; regenerate via `npm run generate:api` | `motifpath-web/CLAUDE.md` — API Client |
| Always use the generated types for request/response shapes; never a manual interface duplicating one | `motifpath-web/CLAUDE.md` — API Client |
| Composition API + `<script setup lang="ts">` only — no Options API | `motifpath-web/CLAUDE.md` — Vue Standards |
| Components never call the API directly — always through a composable or a Pinia store | `motifpath-web/CLAUDE.md` — Vue Standards, State Management |
| Composables live in `src/shared/composables/` and follow the `use` prefix | `motifpath-web/CLAUDE.md` — Vue Standards |
| TypeScript strict mode; no `any`; no `as SomeType` to silence an error — fix the type instead | `motifpath-web/CLAUDE.md` — TypeScript Standards |
| Tailwind utility classes only — no inline `style="..."`, no hardcoded color/spacing outside `tailwind.config.ts` tokens | `motifpath-web/CLAUDE.md` — Styling |
| Named routes only via Vue Router — no hardcoded path strings in components | `motifpath-web/CLAUDE.md` — Key Rules |
| Every data-fetching component handles both loading and error states explicitly | `motifpath-web/CLAUDE.md` — Key Rules |

## Boundaries

- **Public contract:** none exposed outward — this is a consumer of the OpenAPI contract, not a
  producer. A breaking API change originates in `motifpath-specs`, not here.
- **Tolerated legacy:** none documented yet — ask the user before treating anything as
  grandfathered.
- **Automatic gates (don't re-flag):** TypeScript strict-mode compilation; whatever the project's
  lint/format config enforces (confirm exact command on first bootstrap — not yet read).

## Tests

- Vitest + Vue Test Utils; test behavior from the user's perspective — what renders, what happens on
  interaction. Never assert on internal component state or implementation details.
  (`motifpath-web/CLAUDE.md` — Testing)
- No E2E tests at MVP — don't flag their absence as a gap yet; that's a scoped, known limitation.

## Delivery

- Feature/fix/chore branches target `dev`; only `hotfix/BUG-NNN/...` branches from `main` directly.
  (`motifpath-web/CLAUDE.md` — Branching, mirrors the `git` skill)

## Bootstrap notes

Seeded from `motifpath-web/CLAUDE.md` on 2026-08-25 — not yet confirmed against a live review. Exact
lint/format command, CI gate names, and PR template details still need to be read from the repo's
CI config and confirmed with the user on first use.
